import QueryProvider, {
  Hydrate,
  useQuery,
  dehydrate,
  TOKEN_KEY,
  END_POINT,
  useMutation,
  queryClient,
} from './store';

export {
  Hydrate,
  useQuery,
  dehydrate,
  TOKEN_KEY,
  END_POINT,
  useMutation,
  queryClient,
};

export default QueryProvider;
